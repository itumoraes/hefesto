var teamList = $('.new-team-input').closest('.ls-list');

$('[class*="ls-alert"]').remove();

$('.ls-title-intro').after("<%= j render partial: 'shared/flash_messages' %>");

<% if @team.valid? %>
  teamList.before('<%= j render @team %>');
  teamList.remove();
<% end %>

locastyle.dismiss.init();
